RewriteEngine On

# Se o arquivo ou pasta existir de verdade (como seu CSS), abre ele direto
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Se não existir, manda a URL de forma limpa para o index.php
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]